diff --git a/chrome/browser/ui/layout_constants.cc b/chrome/browser/ui/layout_constants.cc
index d89ceaf9e4..f54292a18e 100644
--- a/chrome/browser/ui/layout_constants.cc
+++ b/chrome/browser/ui/layout_constants.cc
@@ -141,10 +141,11 @@ int GetLayoutConstant(LayoutConstant constant) {
       return 20;
     case LayoutConstant::kMainBackgroundRegionCornerRadius:
     case LayoutConstant::kToolbarCornerRadius:
-    case LayoutConstant::kVerticalTabCornerRadius:
       return 8;
+    case LayoutConstant::kVerticalTabCornerRadius:
+      return 9;
     case LayoutConstant::kVerticalTabHeight:
-      return 30;
+      return 42;
     case LayoutConstant::kVerticalTabPinnedHeight:
       return 32;
     case LayoutConstant::kVerticalTabMinWidth:
diff --git a/chrome/browser/ui/views/location_bar/location_bar_view.cc b/chrome/browser/ui/views/location_bar/location_bar_view.cc
index 07fdd70a0b..9ab749ff79 100644
--- a/chrome/browser/ui/views/location_bar/location_bar_view.cc
+++ b/chrome/browser/ui/views/location_bar/location_bar_view.cc
@@ -142,6 +142,7 @@
 #include "ui/events/event.h"
 #include "ui/gfx/animation/slide_animation.h"
 #include "ui/gfx/canvas.h"
+#include "ui/gfx/color_utils.h"
 #include "ui/gfx/geometry/skia_conversions.h"
 #include "ui/gfx/image/image.h"
 #include "ui/gfx/paint_vector_icon.h"
@@ -173,6 +174,9 @@
 
 namespace {
 
+constexpr int kCompactShellLocationBarHeight = 36;
+constexpr int kCompactShellLocationBarRadius = 10;
+
 int IncrementalMinimumWidth(const views::View* view) {
   return (view && view->GetVisible()) ? view->GetMinimumSize().width() : 0;
 }
@@ -491,7 +495,19 @@ bool LocationBarView::IsInitialized() const {
   return is_initialized_;
 }
 
+void LocationBarView::ConfigureForCompactShell() {
+  if (compact_shell_mode_) {
+    return;
+  }
+  compact_shell_mode_ = true;
+  PreferredSizeChanged();
+  RefreshBackground();
+}
+
 int LocationBarView::GetBorderRadius() const {
+  if (compact_shell_mode_) {
+    return kCompactShellLocationBarRadius;
+  }
   return ComputeBorderRadius(size());
 }
 
@@ -661,7 +677,10 @@ bool LocationBarView::HasFocus() const {
 }
 
 gfx::Size LocationBarView::GetMinimumSize() const {
-  const int height = GetLayoutConstant(LayoutConstant::kLocationBarHeight);
+  const int height =
+      compact_shell_mode_
+          ? kCompactShellLocationBarHeight
+          : GetLayoutConstant(LayoutConstant::kLocationBarHeight);
   if (!IsInitialized()) {
     return gfx::Size(0, height);
   }
@@ -688,7 +707,10 @@ gfx::Size LocationBarView::GetMinimumSize() const {
 
 gfx::Size LocationBarView::CalculatePreferredSize(
     const views::SizeBounds& available_size) const {
-  const int height = GetLayoutConstant(LayoutConstant::kLocationBarHeight);
+  const int height =
+      compact_shell_mode_
+          ? kCompactShellLocationBarHeight
+          : GetLayoutConstant(LayoutConstant::kLocationBarHeight);
   if (!IsInitialized()) {
     return gfx::Size(0, height);
   }
@@ -1305,7 +1327,7 @@ int LocationBarView::GetMinimumTrailingWidth() const {
 }
 
 gfx::Rect LocationBarView::GetLocalBoundsWithoutEndcaps() const {
-  const int border_radius = height() / 2;
+  const int border_radius = GetBorderRadius();
   gfx::Rect bounds_without_endcaps(GetLocalBounds());
   bounds_without_endcaps.Inset(gfx::Insets::VH(0, border_radius));
   return bounds_without_endcaps;
@@ -1325,13 +1347,19 @@ void LocationBarView::RefreshBackground() {
   SkColor hovered =
       color_provider->GetColor(kColorLocationBarBackgroundHovered);
 
+  if (compact_shell_mode_ && !high_contrast) {
+    const SkColor toolbar = color_provider->GetColor(kColorToolbar);
+    normal = color_utils::AlphaBlend(SK_ColorWHITE, toolbar, 0.46f);
+    hovered = color_utils::AlphaBlend(SK_ColorWHITE, toolbar, 0.62f);
+  }
+
   background_color_ = gfx::Tween::ColorValueBetween(opacity, normal, hovered);
-  if (is_caret_visible) {
+  if (is_caret_visible && !compact_shell_mode_) {
     // Match the background color to the popup if the Omnibox is visibly
     // focused.
     background_color_ =
         color_provider->GetColor(kColorOmniboxResultsBackground);
-  } else if (input_in_progress && !high_contrast) {
+  } else if (input_in_progress && !high_contrast && !compact_shell_mode_) {
     // Under CR23 guidelines, if the Omnibox is unfocused, but still contains
     // in-progress user input, the background color matches the popup (unless
     // high-contrast mode is enabled).
@@ -1354,13 +1382,24 @@ void LocationBarView::RefreshBackground() {
     border_color = color_provider->GetColor(kColorLocationBarBorderOnMismatch);
   }
 
+  if (compact_shell_mode_ && !high_contrast) {
+    border_color =
+        color_utils::AlphaBlend(SK_ColorWHITE, background_color_, 0.58f);
+  }
+
   if (is_popup_mode_) {
     SetBackground(views::CreateSolidBackground(background_color_));
   } else {
-    SetBackground(CreateRoundRectBackground(
-        background_color_, border_color, size(),
-        /*blend_mode=*/SkBlendMode::kSrcOver,
-        /*antialias=*/true, /*should_border_scale=*/true));
+    const int radius = GetBorderRadius();
+    auto painter = border_color == SK_ColorTRANSPARENT
+                       ? views::Painter::CreateSolidRoundRectPainter(
+                             background_color_, radius, gfx::Insets(),
+                             SkBlendMode::kSrcOver, /*antialias=*/true)
+                       : views::Painter::CreateRoundRectWith1PxBorderPainter(
+                             background_color_, border_color, radius,
+                             SkBlendMode::kSrcOver, /*antialias=*/true,
+                             /*should_border_scale=*/true);
+    SetBackground(CreateBackgroundFromPainter(std::move(painter)));
   }
 
   // Keep the views::Textfield in sync. It needs an opaque background to
@@ -2242,7 +2281,6 @@ void LocationBarView::OnAppShimChanged(const webapps::AppId& app_id) {
 }
 #endif
 
-
 BEGIN_METADATA(LocationBarView)
 ADD_READONLY_PROPERTY_METADATA(int, BorderRadius)
 ADD_READONLY_PROPERTY_METADATA(gfx::Point, OmniboxViewOrigin)
diff --git a/chrome/browser/ui/views/location_bar/location_bar_view.h b/chrome/browser/ui/views/location_bar/location_bar_view.h
index ed7711207f..bbe0e5d2cd 100644
--- a/chrome/browser/ui/views/location_bar/location_bar_view.h
+++ b/chrome/browser/ui/views/location_bar/location_bar_view.h
@@ -140,6 +140,10 @@ class LocationBarView
   // Initializes the LocationBarView.
   void Init();
 
+  // Applies Yee's compact shell presentation without changing omnibox
+  // behavior. The native edit model, popup, and page actions remain intact.
+  void ConfigureForCompactShell();
+
   bool in_popup_state_transition() const override;
 
   // Returns a background that paints an (optionally stroked) rounded rect with
@@ -188,7 +192,6 @@ class LocationBarView
 
   const OmniboxController* GetOmniboxController() const;
 
-
   // Controls the permission dashboard in the LocationBarView.
   PermissionDashboardController* permission_dashboard_controller() {
     return permission_dashboard_controller_.get();
@@ -578,7 +581,6 @@ class LocationBarView
   // Shown if the user has selected a keyword.
   raw_ptr<SelectedKeywordView> selected_keyword_view_ = nullptr;
 
-
   // The content setting views.
   ContentSettingViews content_setting_views_;
 
@@ -602,6 +604,8 @@ class LocationBarView
 
   bool is_initialized_ = false;
 
+  bool compact_shell_mode_ = false;
+
   SkColor background_color_ = gfx::kPlaceholderColor;
 
   // The focus manager associated with this view. The focus manager is expected
diff --git a/chrome/browser/ui/views/tabs/common/tab_view.cc b/chrome/browser/ui/views/tabs/common/tab_view.cc
index 9d955e343c..bcbde1e9de 100644
--- a/chrome/browser/ui/views/tabs/common/tab_view.cc
+++ b/chrome/browser/ui/views/tabs/common/tab_view.cc
@@ -11,6 +11,7 @@
 #include "base/metrics/histogram_functions.h"
 #include "base/metrics/user_metrics.h"
 #include "base/metrics/user_metrics_action.h"
+#include "base/strings/utf_string_conversions.h"
 #include "base/time/time.h"
 #include "chrome/browser/glic/browser_ui/tab_underline_controller.h"
 #include "chrome/browser/glic/browser_ui/tab_underline_view.h"
@@ -65,9 +66,11 @@
 #include "ui/events/types/event_type.h"
 #include "ui/gfx/geometry/insets.h"
 #include "ui/gfx/geometry/rect.h"
+#include "ui/gfx/geometry/rect_f.h"
 #include "ui/gfx/geometry/rounded_corners_f.h"
 #include "ui/gfx/geometry/size.h"
 #include "ui/gfx/geometry/skia_conversions.h"
+#include "ui/native_theme/native_theme.h"
 #include "ui/views/accessibility/view_accessibility.h"
 #include "ui/views/background.h"
 #include "ui/views/border.h"
@@ -84,6 +87,7 @@
 
 namespace {
 constexpr int kIconDesignWidth = 16;
+constexpr int kVerticalIconDesignWidth = 24;
 constexpr int kTitleMinWidth = 10;
 constexpr int kHorizontalInset = 8;
 constexpr int kDefaultPadding = 4;
@@ -130,7 +134,6 @@ TabView::TabView(TabCollectionNode* collection_node)
       collection_node_(collection_node),
       tab_style_(TabStyle::Get()),
       icon_(AddChildView(std::make_unique<TabIcon>())),
-      title_(AddChildView(std::make_unique<TabTitle>())),
       alert_indicator_(
           AddChildView(std::make_unique<AlertIndicatorButton>(this))),
       close_button_(AddChildView(std::make_unique<TabCloseButton>(
@@ -143,6 +146,39 @@ TabView::TabView(TabCollectionNode* collection_node)
                                   this,
                                   kGlowHoverAnimationDuration)
                             : nullptr) {
+  const bool is_vertical =
+      collection_node_->orientation() == TabStripOrientation::kVertical;
+  if (is_vertical) {
+    auto text_container = std::make_unique<views::View>();
+    text_container->SetCanProcessEventsWithinSubtree(false);
+    auto* text_layout =
+        text_container->SetLayoutManager(std::make_unique<views::FlexLayout>());
+    text_layout->SetOrientation(views::LayoutOrientation::kVertical)
+        .SetMainAxisAlignment(views::LayoutAlignment::kCenter)
+        .SetCrossAxisAlignment(views::LayoutAlignment::kStretch);
+
+    title_ = text_container->AddChildView(std::make_unique<TabTitle>());
+    title_->SetFontList(title_->font_list().Derive(-1, gfx::Font::NORMAL,
+                                                   gfx::Font::Weight::MEDIUM));
+    title_->SetLineHeight(13);
+
+    auto subtitle = std::make_unique<views::Label>();
+    subtitle->SetHorizontalAlignment(gfx::ALIGN_TO_HEAD);
+    subtitle->SetElideBehavior(gfx::ELIDE_TAIL);
+    subtitle->SetHandlesTooltips(false);
+    subtitle->SetAutoColorReadabilityEnabled(false);
+    subtitle->SetBackgroundColor(SK_ColorTRANSPARENT);
+    subtitle->SetSkipSubpixelRenderingOpacityCheck(true);
+    subtitle->SetCanProcessEventsWithinSubtree(false);
+    subtitle->SetFontList(subtitle->font_list().Derive(
+        -3, gfx::Font::NORMAL, gfx::Font::Weight::NORMAL));
+    subtitle->SetLineHeight(10);
+    subtitle_ = text_container->AddChildView(std::move(subtitle));
+    text_container_ = AddChildView(std::move(text_container));
+  } else {
+    title_ = AddChildView(std::make_unique<TabTitle>());
+  }
+
   tabs::TabInterface* tab = const_cast<tabs::TabInterface*>(GetTabInterface());
   BrowserWindowInterface* browser_window = tab->GetBrowserWindowInterface();
   if (browser_window &&
@@ -171,10 +207,13 @@ TabView::TabView(TabCollectionNode* collection_node)
       TabChildConfig(alert_indicator_, kIconDesignWidth, kDefaultPadding,
                      /*align_leading=*/false,
                      /*expand=*/false, /*decorate_on_collapse=*/true),
-      TabChildConfig(icon_, kIconDesignWidth, kHorizontalInset,
+      TabChildConfig(icon_,
+                     is_vertical ? kVerticalIconDesignWidth : kIconDesignWidth,
+                     kHorizontalInset,
                      /*align_leading=*/true,
                      /*expand=*/false),
-      TabChildConfig(title_, kTitleMinWidth, kDefaultPadding,
+      TabChildConfig(is_vertical ? text_container_ : title_, kTitleMinWidth,
+                     kDefaultPadding,
                      /*align_leading=*/true,
                      /*expand=*/true)};
 
@@ -544,6 +583,19 @@ void TabView::OnPaint(gfx::Canvas* canvas) {
                            IsHoverAnimationActive(), std::nullopt);
   }
 
+  const bool is_vertical =
+      collection_node_ &&
+      collection_node_->orientation() == TabStripOrientation::kVertical;
+  if (is_vertical && icon_->GetVisible()) {
+    const gfx::Point icon_center = icon_->bounds().CenterPoint();
+    cc::PaintFlags tile_flags;
+    tile_flags.setAntiAlias(true);
+    tile_flags.setColor(SkColorSetARGB(0x52, 0xFF, 0xFF, 0xFF));
+    canvas->DrawRoundRect(gfx::RectF(icon_center.x() - 12.0f,
+                                     icon_center.y() - 12.0f, 24.0f, 24.0f),
+                          7.0f, tile_flags);
+  }
+
   views::View::OnPaint(canvas);
 }
 
@@ -595,7 +647,19 @@ void TabView::PaintTabBackgroundFill(
                                     hovered)) {
     cc::PaintFlags flags;
     flags.setAntiAlias(true);
-    flags.setColor(GetCurrentTabBackgroundColor(selection_state));
+    SkColor background_color = GetCurrentTabBackgroundColor(selection_state);
+    const bool is_vertical =
+        collection_node_ &&
+        collection_node_->orientation() == TabStripOrientation::kVertical;
+    if (is_vertical && GetNativeTheme()->preferred_contrast() !=
+                           ui::NativeTheme::PreferredContrast::kMore) {
+      const SkAlpha alpha =
+          selection_state == TabStyle::TabSelectionState::kActive
+              ? 0x7A
+              : (hovered ? 0x47 : SkColorGetA(background_color));
+      background_color = SkColorSetA(background_color, alpha);
+    }
+    flags.setColor(background_color);
     canvas->DrawRect(GetContentsBounds(), flags);
   }
 
@@ -604,6 +668,18 @@ void TabView::PaintTabBackgroundFill(
         GetThemeProvider()->GetImageSkiaNamed(fill_id.value());
     canvas->TileImageInt(*image, 0, 0, 0, 0, width(), height());
   }
+
+  const bool is_vertical =
+      collection_node_ &&
+      collection_node_->orientation() == TabStripOrientation::kVertical;
+  if (is_vertical && selection_state == TabStyle::TabSelectionState::kActive) {
+    cc::PaintFlags indicator_flags;
+    indicator_flags.setAntiAlias(true);
+    indicator_flags.setColor(SkColorSetRGB(85, 124, 116));
+    canvas->DrawRoundRect(
+        gfx::RectF(0.0f, (height() - 16.0f) * 0.5f, 2.0f, 16.0f), 1.0f,
+        indicator_flags);
+  }
 }
 
 bool TabView::ShouldPaintTabBackgroundColor(
@@ -734,11 +810,9 @@ gfx::Rect TabView::GetChildBounds(const gfx::Rect& container,
   int preferred_height;
   if (config.expand) {
     preferred_width = container.width() - config.padding;
-    // The only expandable view is the views::Label. Just get the line height to
-    // make calculating bounds cheaper.
-    views::Label* label = views::AsViewClass<views::Label>(config.view);
-    CHECK(label);
-    preferred_height = label->GetLineHeight();
+    // Vertical tabs use a compact two-line text container while horizontal
+    // tabs keep the original single Label.
+    preferred_height = config.view->GetPreferredSize().height();
   } else {
     const gfx::Size preferred_size = config.view->GetPreferredSize();
     preferred_width = preferred_size.width();
@@ -765,7 +839,7 @@ gfx::Rect TabView::GetChildBounds(const gfx::Rect& container,
 
 bool TabView::IsChildVisible(const views::View* child_view,
                              const int width) const {
-  if (child_view == title_) {
+  if (child_view == title_ || child_view == text_container_) {
     // Pinned titles should be visible in the expand on hover state when the
     // width is sufficient to show the title.
     return !pinned_ || IsInExpandOnHover(width);
@@ -1088,6 +1162,7 @@ void TabView::UpdateTabData(const tabs::TabInterface* tab) {
   icon_->SetAttention(TabIcon::AttentionType::kTabWantsAttentionStatus,
                       tab_data_.needs_attention);
   UpdateTitle(tab_data_.title, tab_data_.should_render_loading_title);
+  UpdateSubtitle();
   alert_indicator_->TransitionToAlertState(tab_data_.alert_state);
   SetHoverCardDataFrom(tab_data_);
 }
@@ -1104,6 +1179,7 @@ void TabView::SetDataForTesting(tabs::TabData data) {
   icon_->SetAttention(TabIcon::AttentionType::kTabWantsAttentionStatus,
                       tab_data_.needs_attention);
   UpdateTitle(tab_data_.title, tab_data_.should_render_loading_title);
+  UpdateSubtitle();
   alert_indicator_->TransitionToAlertState(tab_data_.alert_state);
   SetHoverCardDataFrom(tab_data_);
 }
@@ -1120,6 +1196,16 @@ void TabView::UpdateTitle(std::u16string title,
   title_->SetText(title);
 }
 
+void TabView::UpdateSubtitle() {
+  if (!subtitle_) {
+    return;
+  }
+
+  const std::string host(tab_data_.visible_url.host());
+  subtitle_->SetText(base::UTF8ToUTF16(host));
+  subtitle_->SetVisible(!host.empty());
+}
+
 void TabView::UpdateBorder() {
   if (pinned_) {
     if (split_) {
@@ -1176,6 +1262,9 @@ void TabView::UpdateColors() {
       GetSelectionState(), IsApparentlyActive(), hovered_, IsFrameActive(),
       GetColorProvider());
   title_->SetEnabledColor(colors.foreground_color);
+  if (subtitle_) {
+    subtitle_->SetEnabledColor(SkColorSetA(colors.foreground_color, 0x98));
+  }
   close_button_->SetColors(colors);
   alert_indicator_->OnParentTabButtonColorChanged();
 
diff --git a/chrome/browser/ui/views/tabs/common/tab_view.h b/chrome/browser/ui/views/tabs/common/tab_view.h
index f92926a409..7d66ff30d6 100644
--- a/chrome/browser/ui/views/tabs/common/tab_view.h
+++ b/chrome/browser/ui/views/tabs/common/tab_view.h
@@ -36,6 +36,10 @@ class TabCollectionNode;
 class TabIcon;
 class TabTitle;
 
+namespace views {
+class Label;
+}  // namespace views
+
 namespace base {
 class TimeDelta;
 }  // namespace base
@@ -188,6 +192,7 @@ class TabView : public views::View,
   void UpdateTabData(const tabs::TabInterface* tab);
 
   void UpdateTitle(std::u16string title, bool should_render_loading_title);
+  void UpdateSubtitle();
   void UpdateBorder();
   void UpdateThemeColors();
   void UpdateColors();
@@ -226,7 +231,9 @@ class TabView : public views::View,
   const raw_ptr<const TabStyle> tab_style_;
 
   const raw_ptr<TabIcon> icon_;
-  const raw_ptr<TabTitle> title_;
+  raw_ptr<views::View> text_container_ = nullptr;
+  raw_ptr<TabTitle> title_ = nullptr;
+  raw_ptr<views::Label> subtitle_ = nullptr;
   const raw_ptr<AlertIndicatorButton> alert_indicator_;
   const raw_ptr<TabCloseButton> close_button_;
   raw_ptr<glic::TabUnderlineView> glic_tab_underline_view_ = nullptr;
diff --git a/chrome/browser/ui/views/toolbar/toolbar_view.cc b/chrome/browser/ui/views/toolbar/toolbar_view.cc
index 6e4a91e46f..1c1dc2f4d6 100644
--- a/chrome/browser/ui/views/toolbar/toolbar_view.cc
+++ b/chrome/browser/ui/views/toolbar/toolbar_view.cc
@@ -380,6 +380,8 @@ void ToolbarView::ConfigureForCompactShell() {
     extensions_container_->GetExtensionsButton()->SetVisible(true);
   }
 
+  location_bar_view_->ConfigureForCompactShell();
+
   constexpr int kControlMargin = 1;
   compact_shell_sidebar_button_->SetProperty(
       views::kMarginsKey, gfx::Insets::VH(0, kControlMargin));
