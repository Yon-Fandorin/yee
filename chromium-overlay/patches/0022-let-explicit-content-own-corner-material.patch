diff --git a/chrome/browser/ui/views/frame/browser_view.cc b/chrome/browser/ui/views/frame/browser_view.cc
index af2e432..19ffb23 100644
--- a/chrome/browser/ui/views/frame/browser_view.cc
+++ b/chrome/browser/ui/views/frame/browser_view.cc
@@ -5272,7 +5272,9 @@ void BrowserView::AddedToWidget() {
         gfx::RoundedCornersF(kYeeContentCornerRadius),
         /*show_top_separator=*/false,
         /*collapsed_vertical_tab_strip_leading_inset=*/kYeeContentGutter,
-        /*vertical_tab_strip_spacing=*/kYeeContentGutter};
+        /*vertical_tab_strip_spacing=*/kYeeContentGutter,
+        /*show_main_background=*/false,
+        /*show_vertical_tab_strip_corners=*/false};
     layout_views.vertical_tab_strip_layout_config =
         BrowserVerticalTabStripLayoutConfig{
             gfx::Insets::TLBR(kYeeTitlebarHeight + kYeeContentGutter, 0,
diff --git a/chrome/browser/ui/views/frame/layout/browser_view_layout.h b/chrome/browser/ui/views/frame/layout/browser_view_layout.h
index 3a0599b..e6859ff 100644
--- a/chrome/browser/ui/views/frame/layout/browser_view_layout.h
+++ b/chrome/browser/ui/views/frame/layout/browser_view_layout.h
@@ -49,6 +49,11 @@ struct BrowserContentLayoutConfig {
   bool show_top_separator = true;
   std::optional<int> collapsed_vertical_tab_strip_leading_inset;
   int vertical_tab_strip_spacing = 0;
+  // Explicit shell surfaces can own the substrate exposed by their rounded
+  // content corners. In that case Chromium's theme-colored background and
+  // vertical-tab corner helpers would paint a second, mismatched material.
+  bool show_main_background = true;
+  bool show_vertical_tab_strip_corners = true;
 };
 
 // Optional placement for a vertical tab strip living inside a shell-defined
diff --git a/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc b/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
index 88465de..9bf915a 100644
--- a/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
+++ b/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
@@ -866,7 +866,11 @@ BrowserViewTabbedLayoutImpl::CalculateProposedLayout(
   if (IsParentedTo(views().vertical_tab_strip_top_corner,
                    views().browser_view)) {
     gfx::Rect corner_bounds;
+    const bool show_vertical_tab_strip_corners =
+        !views().content_layout_config ||
+        views().content_layout_config->show_vertical_tab_strip_corners;
     const bool top_corner_visible =
+        show_vertical_tab_strip_corners &&
         layout_data_->tab_strip_type == TabStripType::kVertical &&
         vertical_tab_strip_animation.top_corner > 0.0;
 
@@ -889,7 +893,11 @@ BrowserViewTabbedLayoutImpl::CalculateProposedLayout(
   if (IsParentedTo(views().vertical_tab_strip_bottom_corner,
                    views().browser_view)) {
     gfx::Rect corner_bounds;
+    const bool show_vertical_tab_strip_corners =
+        !views().content_layout_config ||
+        views().content_layout_config->show_vertical_tab_strip_corners;
     const bool bottom_corner_visible =
+        show_vertical_tab_strip_corners &&
         layout_data_->tab_strip_type == TabStripType::kVertical &&
         vertical_tab_strip_animation.bottom_corner > 0.0;
     if (bottom_corner_visible) {
@@ -994,8 +1002,12 @@ BrowserViewTabbedLayoutImpl::CalculateProposedLayout(
         main_background_bounds.y() > browser_params.visual_client_area.y()) {
       main_background_bounds.Outset(gfx::Outsets::TLBR(1, 0, 0, 0));
     }
-    const bool show_main_background =
-        horizontal_layout.has_side_panel() || adjust_for_cracking;
+    const bool content_allows_main_background =
+        !views().content_layout_config ||
+        views().content_layout_config->show_main_background;
+    const bool show_main_background = content_allows_main_background &&
+                                      (horizontal_layout.has_side_panel() ||
+                                       adjust_for_cracking);
     main_background_layout =
         &layout.AddChild(views().main_background_region, main_background_bounds,
                          show_main_background);
