diff --git a/chrome/browser/ui/views/frame/browser_desktop_window_tree_host_win.cc b/chrome/browser/ui/views/frame/browser_desktop_window_tree_host_win.cc
index 900dccf..646691b 100644
--- a/chrome/browser/ui/views/frame/browser_desktop_window_tree_host_win.cc
+++ b/chrome/browser/ui/views/frame/browser_desktop_window_tree_host_win.cc
@@ -395,6 +395,16 @@ bool BrowserDesktopWindowTreeHostWin::GetClientAreaInsets(
   if (GetWidget()->IsFullscreen()) {
     // In fullscreen mode there is no frame.
     *insets = gfx::Insets();
+  } else if (IsYeeShellScaffoldEnabled() &&
+             browser_view_->GetIsNormalType() &&
+             !GetWidget()->IsMaximized() &&
+             !IsSystemHighContrastEnabled()) {
+    // Paint the restored Yee window all the way to each edge. Leaving native
+    // non-client insets on the left, right, and bottom exposes the dark DWM
+    // resize frame even after its one-pixel accent border is disabled.
+    // BrowserFrameViewWin::NonClientHitTest() supplies equivalent client-area
+    // resize handles for this borderless presentation.
+    *insets = gfx::Insets();
   } else {
     // Reduce the non-client border size; UpdateDWMFrame() will instead extend
     // the border into the window client area. For maximized windows, Windows
diff --git a/chrome/browser/ui/views/frame/browser_frame_view_win.cc b/chrome/browser/ui/views/frame/browser_frame_view_win.cc
index af9f46c..ed1cf73 100644
--- a/chrome/browser/ui/views/frame/browser_frame_view_win.cc
+++ b/chrome/browser/ui/views/frame/browser_frame_view_win.cc
@@ -10,6 +10,7 @@
 #include <memory>
 #include <utility>
 
+#include "base/command_line.h"
 #include "base/memory/raw_ref.h"
 #include "base/scoped_observation.h"
 #include "base/trace_event/common/trace_event_common.h"
@@ -65,6 +66,21 @@ std::array<HICON, BrowserFrameViewWin::kThrobberIconCount>
 
 namespace {
 
+constexpr char kDisableYeeShellScaffoldSwitch[] =
+    "disable-yee-shell-scaffold";
+
+bool IsYeeShellScaffoldEnabled() {
+  return !base::CommandLine::ForCurrentProcess()->HasSwitch(
+      kDisableYeeShellScaffoldSwitch);
+}
+
+bool IsSystemHighContrastEnabled() {
+  HIGHCONTRAST high_contrast = {sizeof(high_contrast)};
+  return ::SystemParametersInfo(SPI_GETHIGHCONTRAST, sizeof(high_contrast),
+                                &high_contrast, 0) &&
+         (high_contrast.dwFlags & HCF_HIGHCONTRASTON) != 0;
+}
+
 // When enabled, a call to BrowserWidget::GetMinimizeButtonOffset() is avoided
 // when not needed. Behind a feature to assess impact
 // (go/chrome-performance-work-should-be-finched).
@@ -375,9 +391,27 @@ int BrowserFrameViewWin::NonClientHitTest(const gfx::Point& point) {
           ? FrameTopBorderThickness(false)
           : GetLayoutConstant(LayoutConstant::kTabStripPadding);
 
+  // Restored Yee windows extend their client surface through the native side
+  // and bottom frame so no dark DWM band is visible. Preserve the corresponding
+  // resize targets inside that client surface. Maximized, fullscreen, and high
+  // contrast windows continue to use the standard Windows frame behavior.
+  const bool use_yee_borderless_resize =
+      IsYeeShellScaffoldEnabled() && GetBrowserView()->GetIsNormalType() &&
+      !IsMaximized() && !browser_widget()->IsFullscreen() &&
+      !IsSystemHighContrastEnabled();
+  const int frame_border_thickness = FrameBorderThickness();
+  const gfx::Insets resize_border =
+      use_yee_borderless_resize
+          ? gfx::Insets::TLBR(top_border_thickness, frame_border_thickness,
+                              frame_border_thickness, frame_border_thickness)
+          : gfx::Insets::TLBR(top_border_thickness, 0, 0, 0);
+  const int resize_corner_width =
+      use_yee_borderless_resize
+          ? kResizeCornerWidth
+          : kResizeCornerWidth - frame_border_thickness;
+
   const int window_component = GetHTComponentForFrame(
-      point, gfx::Insets::TLBR(top_border_thickness, 0, 0, 0),
-      top_border_thickness, kResizeCornerWidth - FrameBorderThickness(),
+      point, resize_border, top_border_thickness, resize_corner_width,
       browser_widget()->widget_delegate()->CanResize());
 
   const int frame_component =
@@ -408,10 +442,12 @@ int BrowserFrameViewWin::NonClientHitTest(const gfx::Point& point) {
     }
 
     // If the clientview registers a hit within its bounds, it's still possible
-    // that the hit target should be top resize since the tabstrip region paints
-    // to the top of the frame. If the frame registered a hit for the Top
-    // resize, override the client frame target.
-    if (window_component == HTTOP && !IsMaximized()) {
+    // that the hit target should be resize. The tabstrip paints into the stock
+    // top resize handle, and Yee's borderless restored frame paints into all
+    // four resize handles. Let those handles override the client target.
+    if ((window_component == HTTOP ||
+         (use_yee_borderless_resize && window_component != HTNOWHERE)) &&
+        !IsMaximized()) {
       return window_component;
     }
     return frame_component;
