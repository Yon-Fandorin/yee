diff --git a/chrome/browser/ui/views/frame/browser_view.cc b/chrome/browser/ui/views/frame/browser_view.cc
index 927ea0f..75dd92d 100644
--- a/chrome/browser/ui/views/frame/browser_view.cc
+++ b/chrome/browser/ui/views/frame/browser_view.cc
@@ -37,7 +37,6 @@
 #include "base/strings/stringprintf.h"
 #include "base/task/sequenced_task_runner.h"
 #include "base/task/single_thread_task_runner.h"
-#include "base/timer/timer.h"
 #include "base/trace_event/trace_event.h"
 #include "build/build_config.h"
 #include "cc/paint/paint_flags.h"
@@ -426,17 +425,12 @@ using web_modal::WebContentsModalDialogHost;
 namespace {
 
 constexpr char kDisableYeeShellScaffoldSwitch[] = "disable-yee-shell-scaffold";
-constexpr char kYeeAgentStatusDemoSwitch[] = "yee-agent-status-demo";
-constexpr char kYeeAgentStatusSwitch[] = "yee-agent-status";
-constexpr int kYeeAgentStatusViewId = 0x594546;
 constexpr int kYeeTitlebarHeight = 48;
 constexpr int kYeeSidebarWidth = 232;
 constexpr int kYeeContentGutter = 6;
 constexpr int kYeeCollapsedSidebarWidth = 8;
 constexpr int kYeeToolbarLeadingInset = 10;
 constexpr int kYeeTitlebarControlGap = 8;
-constexpr int kYeeAgentWidth = 168;
-constexpr int kYeeAgentHeight = 34;
 constexpr float kYeeContentCornerRadius = 8.0f;
 
 bool IsYeeShellScaffoldEnabled() {
@@ -444,131 +438,6 @@ bool IsYeeShellScaffoldEnabled() {
       kDisableYeeShellScaffoldSwitch);
 }
 
-// A compact status view laid out as a first-class titlebar accessory. Its
-// compositor layer keeps status updates local to this surface.
-class YeeAgentStatusView : public views::View {
- public:
-  enum class Status {
-    kReady,
-    kWorking,
-    kNeedsInput,
-  };
-
-  YeeAgentStatusView() {
-    SetCanProcessEventsWithinSubtree(false);
-    SetPreferredSize(gfx::Size(kYeeAgentWidth, kYeeAgentHeight));
-    SetPaintToLayer();
-    layer()->SetFillsBoundsOpaquely(false);
-    layer()->SetName("YeeAgentStatusView");
-
-    const base::CommandLine* const command_line =
-        base::CommandLine::ForCurrentProcess();
-    const std::string requested_status =
-        command_line->GetSwitchValueASCII(kYeeAgentStatusSwitch);
-    if (requested_status == "working") {
-      status_ = Status::kWorking;
-    } else if (requested_status == "needs-input") {
-      status_ = Status::kNeedsInput;
-    }
-
-    if (requested_status.empty() &&
-        command_line->HasSwitch(kYeeAgentStatusDemoSwitch)) {
-      demo_timer_.Start(FROM_HERE, base::Seconds(2.5), this,
-                        &YeeAgentStatusView::AdvanceDemo);
-    }
-  }
-
-  YeeAgentStatusView(const YeeAgentStatusView&) = delete;
-  YeeAgentStatusView& operator=(const YeeAgentStatusView&) = delete;
-  ~YeeAgentStatusView() override = default;
-
-  void OnPaint(gfx::Canvas* canvas) override {
-    const gfx::Rect bounds = GetLocalBounds();
-    if (bounds.IsEmpty()) {
-      return;
-    }
-
-    gfx::RectF surface(bounds);
-    surface.Inset(0.5f);
-    cc::PaintFlags surface_flags;
-    surface_flags.setAntiAlias(true);
-    surface_flags.setColor(SkColorSetRGB(249, 249, 249));
-    surface_flags.setStyle(cc::PaintFlags::kFill_Style);
-    canvas->DrawRoundRect(surface, 6.0f, surface_flags);
-
-    cc::PaintFlags outline_flags;
-    outline_flags.setAntiAlias(true);
-    outline_flags.setColor(SkColorSetRGB(223, 223, 223));
-    outline_flags.setStrokeWidth(1.0f);
-    outline_flags.setStyle(cc::PaintFlags::kStroke_Style);
-    canvas->DrawRoundRect(surface, 6.0f, outline_flags);
-
-    std::u16string_view title;
-    std::u16string_view detail;
-    SkColor status_color;
-    switch (status_) {
-      case Status::kReady:
-        title = u"Ready";
-        detail = u"No active task";
-        status_color = SkColorSetRGB(91, 139, 127);
-        break;
-      case Status::kWorking:
-        title = u"Working";
-        detail = u"Reading this tab";
-        status_color = SkColorSetRGB(65, 156, 132);
-        break;
-      case Status::kNeedsInput:
-        title = u"Needs input";
-        detail = u"Waiting for you";
-        status_color = SkColorSetRGB(190, 132, 78);
-        break;
-    }
-
-    cc::PaintFlags halo_flags;
-    halo_flags.setAntiAlias(true);
-    halo_flags.setColor(SkColorSetA(status_color, 34));
-    halo_flags.setStyle(cc::PaintFlags::kFill_Style);
-    const gfx::PointF indicator_center(17.0f, 17.0f);
-    canvas->DrawCircle(indicator_center, 7.0f, halo_flags);
-
-    cc::PaintFlags indicator_flags;
-    indicator_flags.setAntiAlias(true);
-    indicator_flags.setColor(status_color);
-    indicator_flags.setStyle(cc::PaintFlags::kFill_Style);
-    canvas->DrawCircle(indicator_center, 3.5f, indicator_flags);
-
-    const gfx::FontList title_font = gfx::FontList().Derive(
-        -1, gfx::Font::NORMAL, gfx::Font::Weight::MEDIUM);
-    const gfx::FontList detail_font = gfx::FontList().Derive(
-        -3, gfx::Font::NORMAL, gfx::Font::Weight::NORMAL);
-    canvas->DrawStringRect(
-        title, title_font, SkColorSetRGB(45, 61, 57),
-        gfx::Rect(30, 5, std::max(0, bounds.width() - 40), 13));
-    canvas->DrawStringRect(
-        detail, detail_font, SkColorSetRGB(101, 116, 111),
-        gfx::Rect(30, 18, std::max(0, bounds.width() - 40), 11));
-  }
-
- private:
-  void AdvanceDemo() {
-    switch (status_) {
-      case Status::kReady:
-        status_ = Status::kWorking;
-        break;
-      case Status::kWorking:
-        status_ = Status::kNeedsInput;
-        break;
-      case Status::kNeedsInput:
-        status_ = Status::kReady;
-        break;
-    }
-    SchedulePaint();
-  }
-
-  Status status_ = Status::kReady;
-  base::RepeatingTimer demo_timer_;
-};
-
 // Paint Yee's Windows shell through BrowserView's existing background path.
 // Reading the laid-out content view here keeps the substrate aligned with the
 // real MultiContentsView without a second, manually positioned child view.
@@ -1226,11 +1095,6 @@ BrowserView::BrowserView(Browser* browser)
     vertical_tab_strip_state_controller->SetExpandOnHoverEnabled(true);
 
     SetBackground(std::make_unique<YeeShellBackground>(multi_contents_view_));
-
-    auto yee_agent_status = std::make_unique<YeeAgentStatusView>();
-    auto* const yee_agent_status_view = yee_agent_status.get();
-    AddChildView(std::unique_ptr<views::View>(std::move(yee_agent_status)));
-    yee_agent_status_view->SetID(kYeeAgentStatusViewId);
   }
 
   // Create do-nothing view for the sake of controlling the z-order of the find
@@ -5259,7 +5123,10 @@ void BrowserView::AddedToWidget() {
   layout_views.vertical_tab_strip_top_corner = vertical_tab_strip_top_corner_;
   layout_views.organizer_panel_container = organizer_panel_container_;
   layout_views.toolbar = toolbar_;
-  layout_views.titlebar_end = GetViewByID(kYeeAgentStatusViewId);
+  // The latest pilot folds agent activity into the native toolbar. Reserving a
+  // second titlebar accessory here would shorten the omnibox and collide with
+  // Windows caption controls.
+  layout_views.titlebar_end = nullptr;
   layout_views.infobar_container = infobar_container_;
   layout_views.multi_contents_view = multi_contents_view_;
   layout_views.side_panel = side_panel_;
diff --git a/chrome/browser/ui/views/toolbar/toolbar_view.cc b/chrome/browser/ui/views/toolbar/toolbar_view.cc
index d30568d..d1f5907 100644
--- a/chrome/browser/ui/views/toolbar/toolbar_view.cc
+++ b/chrome/browser/ui/views/toolbar/toolbar_view.cc
@@ -19,6 +19,7 @@
 #include "base/metrics/user_metrics_action.h"
 #include "base/notimplemented.h"
 #include "base/strings/utf_string_conversions.h"
+#include "base/timer/timer.h"
 #include "base/trace_event/trace_event.h"
 #include "build/build_config.h"
 #include "chrome/app/chrome_command_ids.h"
@@ -213,6 +214,130 @@ constexpr int kInsideBorderAroundGlicButtons = 2;
 constexpr int kOutsideBorderAroundGlicButtons = 11;
 constexpr int kGlicButtonMargin = 5;
 
+constexpr char kYeeAgentStatusDemoSwitch[] = "yee-agent-status-demo";
+constexpr char kYeeAgentStatusSwitch[] = "yee-agent-status";
+
+// The pilot's agent activity is a compact toolbar affordance, not a second
+// titlebar surface. Keeping it as a native ToolbarButton preserves keyboard,
+// hover, focus, and caption hit-testing behavior.
+class YeeAgentToolbarButton : public ToolbarButton {
+ public:
+  enum class Status {
+    kReady,
+    kWorking,
+    kNeedsInput,
+  };
+
+  explicit YeeAgentToolbarButton(PressedCallback callback)
+      : ToolbarButton(std::move(callback)) {
+    const base::CommandLine* const command_line =
+        base::CommandLine::ForCurrentProcess();
+    const std::string requested_status =
+        command_line->GetSwitchValueASCII(kYeeAgentStatusSwitch);
+    if (requested_status == "working") {
+      status_ = Status::kWorking;
+    } else if (requested_status == "needs-input") {
+      status_ = Status::kNeedsInput;
+    }
+    UpdateAccessibleText();
+
+    if (requested_status.empty() &&
+        command_line->HasSwitch(kYeeAgentStatusDemoSwitch)) {
+      demo_timer_.Start(FROM_HERE, base::Seconds(2.5), this,
+                        &YeeAgentToolbarButton::AdvanceDemo);
+    }
+  }
+
+  YeeAgentToolbarButton(const YeeAgentToolbarButton&) = delete;
+  YeeAgentToolbarButton& operator=(const YeeAgentToolbarButton&) = delete;
+  ~YeeAgentToolbarButton() override = default;
+
+ protected:
+  void PaintButtonContents(gfx::Canvas* canvas) override {
+    ToolbarButton::PaintButtonContents(canvas);
+
+    SkColor status_color = SkColorSetRGB(91, 148, 134);
+    if (status_ == Status::kNeedsInput) {
+      status_color = SkColorSetRGB(190, 132, 78);
+    }
+
+    cc::PaintFlags halo_flags;
+    halo_flags.setAntiAlias(true);
+    halo_flags.setColor(SkColorSetA(status_color, 34));
+    halo_flags.setStyle(cc::PaintFlags::kFill_Style);
+    canvas->DrawCircle(gfx::PointF(15.0f, 15.0f), 7.0f, halo_flags);
+
+    cc::PaintFlags pulse_flags;
+    pulse_flags.setAntiAlias(true);
+    pulse_flags.setColor(status_color);
+    pulse_flags.setStyle(cc::PaintFlags::kFill_Style);
+    canvas->DrawCircle(gfx::PointF(15.0f, 15.0f), 4.0f, pulse_flags);
+
+    if (status_ == Status::kReady) {
+      return;
+    }
+
+    gfx::RectF badge_rect(17.0f, 1.0f, 13.0f, 13.0f);
+    cc::PaintFlags badge_border_flags;
+    badge_border_flags.setAntiAlias(true);
+    badge_border_flags.setColor(SkColorSetRGB(243, 243, 243));
+    badge_border_flags.setStyle(cc::PaintFlags::kFill_Style);
+    canvas->DrawRoundRect(badge_rect, 6.5f, badge_border_flags);
+
+    badge_rect.Inset(1.5f);
+    cc::PaintFlags badge_flags;
+    badge_flags.setAntiAlias(true);
+    badge_flags.setColor(status_ == Status::kWorking
+                             ? SkColorSetRGB(15, 108, 92)
+                             : SkColorSetRGB(166, 105, 48));
+    badge_flags.setStyle(cc::PaintFlags::kFill_Style);
+    canvas->DrawRoundRect(badge_rect, 5.0f, badge_flags);
+
+    const gfx::FontList badge_font = gfx::FontList().Derive(
+        -5, gfx::Font::NORMAL, gfx::Font::Weight::BOLD);
+    canvas->DrawStringRectWithFlags(
+        status_ == Status::kWorking ? u"2" : u"!", badge_font, SK_ColorWHITE,
+        gfx::Rect(18, 1, 11, 12), gfx::Canvas::TEXT_ALIGN_CENTER);
+  }
+
+ private:
+  void AdvanceDemo() {
+    switch (status_) {
+      case Status::kReady:
+        status_ = Status::kWorking;
+        break;
+      case Status::kWorking:
+        status_ = Status::kNeedsInput;
+        break;
+      case Status::kNeedsInput:
+        status_ = Status::kReady;
+        break;
+    }
+    UpdateAccessibleText();
+    SchedulePaint();
+  }
+
+  void UpdateAccessibleText() {
+    std::u16string label;
+    switch (status_) {
+      case Status::kReady:
+        label = u"Agent activity, ready";
+        break;
+      case Status::kWorking:
+        label = u"Agent activity, 2 findings ready";
+        break;
+      case Status::kNeedsInput:
+        label = u"Agent activity, needs input";
+        break;
+    }
+    SetTooltipText(label);
+    GetViewAccessibility().SetName(label);
+  }
+
+  Status status_ = Status::kReady;
+  base::RepeatingTimer demo_timer_;
+};
+
 // Returns whether `point` should be treated as part of the caption area in
 // `view`. Recursively traverses into icon containers to correctly handle
 // padding between buttons.
@@ -325,6 +450,37 @@ void ToolbarView::ConfigureForCompactShell() {
       &ToolbarView::ToggleCompactShellSidebar, base::Unretained(this)));
   sidebar_button->SetVectorIcon(kDockToLeftIcon);
   compact_shell_sidebar_button_ = AddChildViewAt(std::move(sidebar_button), 0);
+
+  const auto compact_shell_command = [](Browser* browser, int command,
+                                        const ui::Event& event) {
+    chrome::ExecuteCommandWithDisposition(
+        browser, command, ui::DispositionFromEventFlags(event.flags()));
+  };
+  auto new_tab_button = std::make_unique<ToolbarButton>(base::BindRepeating(
+      compact_shell_command, browser_, IDC_NEW_TAB));
+  new_tab_button->SetVectorIcon(kAddIcon);
+  const std::u16string new_tab_label =
+      l10n_util::GetStringUTF16(IDS_TOOLTIP_NEW_TAB);
+  new_tab_button->SetTooltipText(new_tab_label);
+  new_tab_button->GetViewAccessibility().SetName(new_tab_label);
+  ToolbarButton* const compact_shell_new_tab_button =
+      AddChildViewAt(std::move(new_tab_button), 1);
+
+  const auto show_agent_activity = [](Browser* browser,
+                                      const ui::Event& event) {
+    auto* const controller =
+        tabs::VerticalTabStripStateController::From(browser);
+    if (controller && controller->GetCollapseState() !=
+                          tabs::VerticalTabStripCollapseState::kExpanded) {
+      chrome::ExecuteCommandWithDisposition(
+          browser, IDC_TOGGLE_VERTICAL_TABS_COLLAPSE,
+          ui::DispositionFromEventFlags(event.flags()));
+    }
+  };
+  auto agent_button = std::make_unique<YeeAgentToolbarButton>(
+      base::BindRepeating(show_agent_activity, browser_));
+  ToolbarButton* const compact_shell_agent_button = AddChildViewAt(
+      std::unique_ptr<ToolbarButton>(std::move(agent_button)), 2);
   compact_shell_sidebar_subscription_ =
       vertical_tabs_controller->RegisterOnCollapseChanged(
           base::BindRepeating(&ToolbarView::UpdateCompactShellSidebarButton,
@@ -384,6 +540,8 @@ void ToolbarView::ConfigureForCompactShell() {
 
   const gfx::Size kCompactControlSize(30, 30);
   compact_shell_sidebar_button_->SetPreferredSize(kCompactControlSize);
+  compact_shell_new_tab_button->SetPreferredSize(kCompactControlSize);
+  compact_shell_agent_button->SetPreferredSize(kCompactControlSize);
   if (back_) {
     back_->SetPreferredSize(kCompactControlSize);
   }
@@ -401,6 +559,10 @@ void ToolbarView::ConfigureForCompactShell() {
   constexpr int kControlMargin = 1;
   compact_shell_sidebar_button_->SetProperty(
       views::kMarginsKey, gfx::Insets::VH(0, kControlMargin));
+  compact_shell_new_tab_button->SetProperty(
+      views::kMarginsKey, gfx::Insets::VH(0, kControlMargin));
+  compact_shell_agent_button->SetProperty(
+      views::kMarginsKey, gfx::Insets::VH(0, kControlMargin));
   if (back_) {
     back_->SetProperty(views::kMarginsKey, gfx::Insets::VH(0, kControlMargin));
   }
