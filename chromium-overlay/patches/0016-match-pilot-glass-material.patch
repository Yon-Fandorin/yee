diff --git a/chrome/browser/ui/views/frame/browser_view.cc b/chrome/browser/ui/views/frame/browser_view.cc
--- a/chrome/browser/ui/views/frame/browser_view.cc
+++ b/chrome/browser/ui/views/frame/browser_view.cc
@@ -616,8 +616,8 @@ class YeeContextSwitcherView : public views::View {
 };
 
 // A deliberately content-free shell used while Yee's top-level regions are
-// being designed. The existing Chromium views stay alive beneath this overlay
-// so the experiment remains reversible and does not fork browser behavior.
+// being designed. Existing Chromium views paint above this material layer, so
+// the experiment remains reversible and does not fork browser behavior.
 class YeeShellScaffoldView : public views::View {
  public:
   YeeShellScaffoldView() {
@@ -645,26 +645,32 @@ class YeeShellScaffoldView : public views::View {
       return;
     }
 
-    // These low-alpha surfaces allow the native GlassFrame material to remain
-    // visible without imposing a decorative gradient on the shell.
+    // Windows does not expose Chromium's macOS-only GlassFrame material. Paint
+    // the pilot's porcelain base below the native controls so translucent
+    // surfaces never reveal the active frame's saturated accent color.
+    canvas->FillRect(bounds, SkColorSetRGB(238, 241, 239));
+
+    // Match the pilot's frosted titlebar and sidebar tints. The shell view is
+    // installed below the interactive Chromium views, so these fills provide
+    // material without washing out toolbar or tab content.
     canvas->FillRect(gfx::Rect(0, 0, bounds.width(), kYeeTitlebarHeight),
-                     SkColorSetARGB(38, 225, 235, 232));
+                     SkColorSetARGB(224, 246, 249, 247));
     const int sidebar_width =
         base::ClampRound(kYeeSidebarWidth * sidebar_reveal_);
     if (sidebar_width > 0) {
       canvas->FillRect(
           gfx::Rect(0, kYeeTitlebarHeight, sidebar_width,
                     std::max(0, bounds.height() - kYeeTitlebarHeight)),
-          SkColorSetARGB(31, 225, 235, 232));
+          SkColorSetARGB(158, 232, 240, 237));
     }
 
     cc::PaintFlags surface_flags;
     surface_flags.setAntiAlias(true);
-    surface_flags.setColor(SkColorSetARGB(31, 255, 255, 255));
+    surface_flags.setColor(SkColorSetARGB(117, 255, 255, 255));
     surface_flags.setStyle(cc::PaintFlags::kFill_Style);
 
     cc::PaintFlags outline_flags;
     outline_flags.setAntiAlias(true);
-    outline_flags.setColor(SkColorSetARGB(82, 255, 255, 255));
+    outline_flags.setColor(SkColorSetARGB(148, 255, 255, 255));
     outline_flags.setStrokeWidth(1.0f);
     outline_flags.setStyle(cc::PaintFlags::kStroke_Style);
@@ -689,7 +697,7 @@ class YeeShellScaffoldView : public views::View {
                   top_height));
     cc::PaintFlags content_outline_flags;
     content_outline_flags.setAntiAlias(true);
-    content_outline_flags.setColor(SkColorSetARGB(52, 74, 110, 101));
+    content_outline_flags.setColor(SkColorSetARGB(41, 54, 93, 85));
     content_outline_flags.setStrokeWidth(1.0f);
     content_outline_flags.setStyle(cc::PaintFlags::kStroke_Style);
     const int content_left = kYeeShellGutter + sidebar_width;
@@ -1311,7 +1319,11 @@ BrowserView::BrowserView(Browser* browser)
 
     auto yee_shell_scaffold = std::make_unique<YeeShellScaffoldView>();
     auto* const yee_shell_scaffold_view = yee_shell_scaffold.get();
-    AddChildView(std::unique_ptr<views::View>(std::move(yee_shell_scaffold)));
+    // Keep the material underneath WebContents, toolbar, and tabs. Painting it
+    // as the first BrowserView child lets it replace the raw frame color while
+    // preserving native interaction and text contrast above it.
+    AddChildViewAt(
+        std::unique_ptr<views::View>(std::move(yee_shell_scaffold)), 0);
     yee_shell_scaffold_view->SetID(kYeeShellScaffoldViewId);
     yee_shell_scaffold_view->SetProperty(views::kViewIgnoredByLayoutKey, true);
 
diff --git a/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc b/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
--- a/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
+++ b/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
@@ -1507,6 +1507,13 @@ void BrowserViewTabbedLayoutImpl::DoPostLayoutVisualAdjustments(
   // Set vertical tabstrip corners.
   CustomCorners::ColorChoiceWithAlpha frame_color(CustomCorners::FrameTheme(),
                                                   1.0f);
+  if (views().content_layout_config) {
+    // Yee's shell uses the toolbar palette as a translucent porcelain tint.
+    // Keeping this independent of the active frame color prevents the Windows
+    // accent color from leaking into the tab rail and content gutter.
+    frame_color = CustomCorners::ColorChoiceWithAlpha(
+        CustomCorners::ToolbarTheme(), 0.72f);
+  }
   if (layout_data_->tab_strip_type == TabStripType::kVertical) {
     side_panel_start = views().vertical_tab_strip_region_view->bounds().right();
 
@@ -1711,6 +1718,14 @@ void BrowserViewTabbedLayoutImpl::DoPostLayoutVisualAdjustments(
             views().vertical_tab_strip_region_view, blur_background_opacity);
       }
     }
+    if (views().content_layout_config) {
+      // Chromium's GlassFrame is macOS-only. The Views compositor supports a
+      // portable backdrop blur, which gives Yee's floating sidebar the same
+      // frosted treatment when it moves over page content on Windows.
+      vertical_tabs_background->SetUseBackgroundBlur(true);
+      views().vertical_tab_strip_region_view->layer()->SetBackgroundBlur(
+          18.0f);
+    }
   } else if (layout_data_->tab_strip_type == TabStripType::kHorizontal &&
              !is_fullscreen(layout_data_->window_state) && in_glass_mode()) {
     frame_color.opacity = 0.0f;
@@ -1721,6 +1736,11 @@ void BrowserViewTabbedLayoutImpl::DoPostLayoutVisualAdjustments(
       views().toolbar->background()->AsA<CustomCornersBackground>();
   CHECK(toolbar_background)
       << "Expected toolbar to have a CustomCornersBackground.";
+  if (views().toolbar_layout_config) {
+    toolbar_background->SetPrimaryColor(
+        CustomCorners::ColorChoiceWithAlpha(CustomCorners::ToolbarTheme(),
+                                            0.88f));
+  }
 
   // Set toolbar corners.
   CustomCornersBackground::Corners toolbar_corners;
@@ -1772,6 +1792,11 @@ void BrowserViewTabbedLayoutImpl::DoPostLayoutVisualAdjustments(
       views().top_container->background()->AsA<CustomCornersBackground>();
   CHECK(top_container_background)
       << "Expected top container to have a CustomCornersBackground.";
+  if (views().toolbar_layout_config) {
+    top_container_background->SetPrimaryColor(
+        CustomCorners::ColorChoiceWithAlpha(CustomCorners::ToolbarTheme(),
+                                            0.88f));
+  }
 
   // Set up top container cutouts. These need to be updated whether or not glass
   // is active for the current window.
