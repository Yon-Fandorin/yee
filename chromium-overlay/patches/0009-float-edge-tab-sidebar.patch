diff --git a/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc b/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
--- a/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
+++ b/chrome/browser/ui/views/frame/layout/browser_view_tabbed_layout_impl.cc
@@ -44,6 +44,7 @@
 #include "ui/gfx/geometry/outsets.h"
 #include "ui/gfx/geometry/rounded_corners_f.h"
 #include "ui/gfx/geometry/size.h"
+#include "ui/gfx/geometry/transform.h"
 #include "ui/views/controls/separator.h"
 #include "ui/views/view_class_properties.h"
 #include "ui/views/view_utils.h"
@@ -823,6 +824,10 @@
 
   if (IsParentedTo(views().vertical_tab_strip_background_blur_backdrop,
                    views().browser_view)) {
+    if (views().vertical_tab_strip_layout_config) {
+      views().vertical_tab_strip_background_blur_backdrop->SetCollapsedWidth(
+          views().vertical_tab_strip_layout_config->collapsed_width);
+    }
     layout.AddChild(
         views().vertical_tab_strip_background_blur_backdrop,
         vertical_tab_strip_bounds,
@@ -1508,6 +1513,24 @@
     // collapsed or there are no caption buttons on the leading edge.
     const VerticalTabStripAnimation& animation =
         layout_data_->vertical_tab_strip_animation;
+    const BrowserVerticalTabStripLayoutConfig* const tab_strip_config =
+        views().vertical_tab_strip_layout_config
+            ? &*views().vertical_tab_strip_layout_config
+            : nullptr;
+    const int flyout_offset =
+        tab_strip_config ? base::ClampRound(tab_strip_config->collapsed_width *
+                                            animation.expand_on_hover_width)
+                         : 0;
+    gfx::Transform flyout_transform;
+    flyout_transform.Translate(
+        base::i18n::IsRTL() ? -flyout_offset : flyout_offset, 0);
+    views().vertical_tab_strip_region_view->layer()->SetTransform(
+        flyout_transform);
+    if (views().vertical_tab_strip_background_blur_backdrop) {
+      views()
+          .vertical_tab_strip_background_blur_backdrop->layer()
+          ->SetTransform(flyout_transform);
+    }
     auto* const vertical_tabs_background =
         views()
             .vertical_tab_strip_region_view->background()
@@ -1569,6 +1592,23 @@
                            vertical_tabs_bottom_corner_amount);
       vertical_tabs_corners[CornerOrientation::kBottomTrailing].radius =
           vertical_tabs_bottom_corner_size;
+    }
+
+    if (tab_strip_config && tab_strip_config->collapsed_width > 0 &&
+        animation.expand_on_hover_width > 0.0) {
+      const int radius =
+          base::ClampRound((tab_strip_config->collapsed_width + 5) *
+                           animation.expand_on_hover_width);
+      for (const CornerOrientation corner :
+           {CornerOrientation::kTopLeading, CornerOrientation::kTopTrailing,
+            CornerOrientation::kBottomTrailing,
+            CornerOrientation::kBottomLeading}) {
+        vertical_tabs_corners[corner].type =
+            CustomCornersBackground::CornerType::kRounded;
+        vertical_tabs_corners[corner].radius = radius;
+      }
+      vertical_tabs_bottom_corner_amount = animation.expand_on_hover_width;
+      vertical_tabs_bottom_corner_size = radius;
     }
 
     vertical_tabs_background->SetCorners(vertical_tabs_corners);
@@ -1628,6 +1668,7 @@
       vertical_tabs_outline.top = true;
     }
     if (animation.expand_on_hover_width > 0.0) {
+      vertical_tabs_outline.leading = flyout_offset > 0;
       vertical_tabs_outline.bottom = true;
     }
     vertical_tabs_background->SetOutline(vertical_tabs_outline);
diff --git a/chrome/browser/ui/views/frame/vertical_tab_strip_background_blur_backdrop.cc b/chrome/browser/ui/views/frame/vertical_tab_strip_background_blur_backdrop.cc
--- a/chrome/browser/ui/views/frame/vertical_tab_strip_background_blur_backdrop.cc
+++ b/chrome/browser/ui/views/frame/vertical_tab_strip_background_blur_backdrop.cc
@@ -4,6 +4,8 @@
 
 #include "chrome/browser/ui/views/frame/vertical_tab_strip_background_blur_backdrop.h"
 
+#include <algorithm>
+
 #include "cc/paint/paint_flags.h"
 #include "chrome/browser/ui/color/chrome_color_id.h"
 #include "chrome/browser/ui/views/frame/custom_corners_background.h"
@@ -19,6 +21,7 @@
   SetCanProcessEventsWithinSubtree(false);
   SetPaintToLayer();
   layer()->SetFillsBoundsOpaquely(false);
+  collapsed_width_ = VerticalTabStripRegionView::kCollapsedWidth;
 }
 
 VerticalTabStripBackgroundBlurBackdrop::
@@ -35,6 +38,16 @@
   }
 }
 
+void VerticalTabStripBackgroundBlurBackdrop::SetCollapsedWidth(
+    int collapsed_width) {
+  collapsed_width = std::max(0, collapsed_width);
+  if (collapsed_width_ == collapsed_width) {
+    return;
+  }
+  collapsed_width_ = collapsed_width;
+  SchedulePaint();
+}
+
 void VerticalTabStripBackgroundBlurBackdrop::OnPaint(gfx::Canvas* canvas) {
   View::OnPaint(canvas);
 
@@ -53,7 +66,7 @@
   auto color = GetColorProvider()->GetColor(kColorToolbar);
   color = SkColorSetA(color, base::ClampRound(alpha_ * 255.0f));
   gfx::Rect rect = GetLocalBounds();
-  rect.set_width(VerticalTabStripRegionView::kCollapsedWidth);
+  rect.set_width(std::min(collapsed_width_, rect.width()));
   rect = GetMirroredRect(rect);
   canvas->FillRect(rect, color);
 
@@ -62,9 +75,10 @@
   // over. This partly mimics the MacOS edge effect on window frames in Glass
   // mode.
   canvas->ClipRect(rect, SkClipOp::kDifference);
-  constexpr float kEdgeStrokeWidth = 4.0f;
+  const float edge_stroke_width =
+      std::clamp(collapsed_width_ * 0.125f, 1.0f, 4.0f);
   cc::PaintFlags flags;
-  flags.setStrokeWidth(2.0f * kEdgeStrokeWidth);
+  flags.setStrokeWidth(2.0f * edge_stroke_width);
   flags.setAntiAlias(true);
   flags.setColor(color);
   flags.setStyle(cc::PaintFlags::kStroke_Style);
diff --git a/chrome/browser/ui/views/frame/vertical_tab_strip_background_blur_backdrop.h b/chrome/browser/ui/views/frame/vertical_tab_strip_background_blur_backdrop.h
--- a/chrome/browser/ui/views/frame/vertical_tab_strip_background_blur_backdrop.h
+++ b/chrome/browser/ui/views/frame/vertical_tab_strip_background_blur_backdrop.h
@@ -21,12 +21,14 @@
 
   // Call after the vertical tab strip background is configured.
   void UpdateGeometry(const VerticalTabStripRegionView* from, float alpha);
+  void SetCollapsedWidth(int collapsed_width);
 
   // views::View:
   void OnPaint(gfx::Canvas* canvas) override;
 
  private:
   float alpha_ = 1.0f;
+  int collapsed_width_ = 0;
   SkPath border_path_;
 };
 
